## TTS Clone Studio 2.1.0

### Mới
- 🔄 **Tự cập nhật**: ứng dụng tự kiểm tra bản mới trên GitHub khi mở (và 6 giờ/lần). Có bản mới sẽ hiện nút **⬆ Có bản mới** — bấm là tự tải, kiểm tra mã SHA-256, cài và mở lại. Giọng, cài đặt và API key được giữ nguyên.
- 🪟 **Windows**: bộ cài `…-windows-setup.exe` (không cần quyền quản trị, tạo lối tắt Desktop/Start) và bản chạy 1 file `…-windows-portable.exe`.
- 🍎 **macOS**: `…-mac-arm64.zip` (Apple Silicon M1/M2/M3/M4) và `…-mac-x64.zip` (Intel).
- 🎬 Xuất video MP4, chạy hàng loạt Excel đa luồng, giao diện Sáng/Tối (từ bản 2.0).

### Tải bản nào?
| Máy | File |
|---|---|
| Windows (khuyên dùng) | `TTSCloneStudio-2.1.0-windows-setup.exe` |
| Windows, không muốn cài | `TTSCloneStudio-2.1.0-windows-portable.exe` |
| Mac chip Apple (M1–M4) | `TTSCloneStudio-2.1.0-mac-arm64.zip` |
| Mac chip Intel | `TTSCloneStudio-2.1.0-mac-x64.zip` |

Lần đầu mở trên Windows: nếu SmartScreen báo *Unknown publisher* → **More info › Run anyway**.
Lần đầu mở trên Mac: xem hướng dẫn trong README (mục macOS).
