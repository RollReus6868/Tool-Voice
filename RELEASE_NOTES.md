## TTS Clone Studio 2.2.0

### Mới
- ☁ **Duyệt giọng Inworld**: nút ☁ ngay cạnh ô chọn giọng mở bảng toàn bộ giọng trên Inworld (của bạn + hệ thống). Lọc theo ngôn ngữ, giới tính, tìm theo tên/mô tả, **▶ Nghe thử**, **🗣 Dùng ngay**.
- 🔄 **Đồng bộ Inworld**: thêm vào thư viện mọi giọng bạn đã clone trên Inworld; có tùy chọn tự đồng bộ mỗi lần mở app.
- 🎭 **Delivery như Inworld Playground**: Ổn định / Cân bằng / Sáng tạo, ✨ khử nhiễu (tăng chất lượng) và 💬 chỉ dẫn phong cách đọc (model inworld-tts-2). Có ở cả trang Đọc văn bản và Hàng loạt.
- 🧠 Model mặc định đổi sang **inworld-tts-2-flash** (nhanh, rẻ hơn).
- 🔢 **Chạy hàng loạt theo số thứ tự**: gõ `1-10, 15, 20-` để chỉ chạy các dòng đó. Bảng có thêm cột STT.
- 💳 **Gói & mức dùng Inworld**: chọn gói, nhập số dư; app tự đếm ký tự đã dùng, tiền đã dùng, tiền và ký tự còn lại, hiện % bằng thanh màu và nút trên thanh tiêu đề. Ước tính chi phí trước khi đọc.

### Lưu ý
- Inworld chưa có API để đọc số dư, nên số liệu do app tự tính từ các lần đọc trong app; đối chiếu với trang Billing khi cần.

### Tải bản nào?
| Máy | File |
|---|---|
| Windows (khuyên dùng) | `TTSCloneStudio-2.2.0-windows-setup.exe` |
| Windows, không muốn cài | `TTSCloneStudio-2.2.0-windows-portable.exe` |
| Mac chip Apple (M1–M4) | `TTSCloneStudio-2.2.0-mac-arm64.zip` |
| Mac chip Intel | `TTSCloneStudio-2.2.0-mac-x64.zip` |

Đang dùng bản 2.1.0 thì không cần tải: app sẽ hiện nút **⬆ Có bản mới** và tự cài.
